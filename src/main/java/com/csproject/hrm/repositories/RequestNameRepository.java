package com.csproject.hrm.repositories;

import com.csproject.hrm.entities.RequestName;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RequestNameRepository extends JpaRepository<RequestName, Long> {}
